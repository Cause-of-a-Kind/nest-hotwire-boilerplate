<turbo-stream action="after" target="message_{{previousMessageId}}">
  <template>
    <turbo-frame id="message_{{message.id}}">
      {{> message message=message}}
    </turbo-frame>
  </template>
</turbo-stream>